# Benchmarks

This folder contains the benchmark server implementations used in experiments.

- `c/`: C server implementations (Windows and Unix variants, plus older server versions)
- `erlang/`: Erlang echo server/client modules and older variants
- `java/`: Java server implementations

These files are manually written source code. Compiled artifacts should not be committed.
