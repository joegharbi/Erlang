# Java benchmark sources

Purpose: Java echo server implementations for comparison experiments.

- `SimpleServer.java`: main server used in measurement runs
- `EchoServer1.java`: older implementation kept for archive/history

Generated `.class` files are build artifacts and should stay out of version control.
