# C benchmark sources

Purpose: C server implementations used in benchmark runs.

- `c_server_win.c`: primary Windows echo server used by measurement scripts
- `c_server_unix.c`: Unix variant
- `server.c`, `server_c.c`: older/alternate implementations kept for traceability

Status: active + legacy variants mixed; keep all files because some old results refer to these implementations.
