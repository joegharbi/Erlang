# Erlang benchmark sources

Purpose: Erlang echo server/client modules used in benchmark experiments.

Important files:
- `echo_server_prod.erl`: main server module used by measurement scripts
- `echo_client_prod.erl`: production-style client module
- other `.erl` files: legacy variants and exploratory modules used in earlier iterations

Status: active benchmark module plus archived legacy modules for historical reproducibility.
