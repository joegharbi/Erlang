# Analysis scripts

Purpose: post-process benchmark CSV outputs.

- `norm.py`: normalizes measurements against C per condition
- `ration.py`: computes derived ratio columns from `data.csv`
- `consumption.py`: utility script for JSON consumption inspection

These scripts read/write under `results/benchmark_outputs/`.
