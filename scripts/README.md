# Scripts

This folder groups automation scripts by purpose:

- `measurements/`: run benchmarks and append energy/runtime CSV outputs
- `analysis/`: transform benchmark CSV files into normalized or derived tables
- `legacy/`: older exploratory scripts kept for reference

Most measurement scripts target Windows + Scaphandre workflows.
