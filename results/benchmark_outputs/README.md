# Benchmark outputs (active)

Tracked CSV outputs used in analyses and paper material.

Includes:
- per-language output CSVs (`c_output*.csv`, `erlang_output*.csv`, `java_output*.csv`)
- aggregate/derived tables (`data.csv`, `detailed_data.csv`, `ration1.csv`)
- additional historical CSV outputs retained for reproducibility

Status: active, publication-relevant.
